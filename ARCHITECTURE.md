# 🏛️ Architecture Documentation

## Library Management System

---

## C1 – Context Diagram

ระบบ **Library Management System** เป็นระบบ Backend API ที่ให้บริการจัดการข้อมูลหนังสือในห้องสมุด
โดยผู้ใช้งาน (Client) จะเรียกใช้งานผ่าน HTTP Request (เช่น Browser, Postman, curl)

**External Actors**

* Client / User
* REST API Consumer

**System**

* Library Management System (Node.js + Express)

**Database**

* SQLite Database

ภาพรวมการทำงาน:
Client → Library Management System → SQLite Database

---

## C2 – Container Diagram (Layered Architecture)

ระบบถูกออกแบบตาม **Layered Architecture (3-tier)** โดยแยกความรับผิดชอบของแต่ละส่วนอย่างชัดเจน

```
┌─────────────────────────────────────┐
│     Presentation Layer              │
│  ┌──────────────────────────────┐   │
│  │ Routes → Controllers         │   │
│  │ (HTTP Handling)              │   │
│  └──────────────────────────────┘   │
└──────────────┬──────────────────────┘
               │
               ▼
┌─────────────────────────────────────┐
│     Business Logic Layer            │
│  ┌──────────────────────────────┐   │
│  │ Services → Validators        │   │
│  │ (Business Rules)             │   │
│  └──────────────────────────────┘   │
└──────────────┬──────────────────────┘
               │
               ▼
┌─────────────────────────────────────┐
│     Data Access Layer               │
│  ┌──────────────────────────────┐   │
│  │ Repositories → Database      │   │
│  │ (SQL Queries)                │   │
│  └──────────────────────────────┘   │
└──────────────┬──────────────────────┘
               │
               ▼
          ┌──────────┐
          │  SQLite  │
          └──────────┘
```

---

## Responsibilities ของแต่ละ Layer

### 1️⃣ Presentation Layer

**โฟลเดอร์:** `src/presentation`

**Responsibilities:**

* รับ HTTP Request จาก Client
* กำหนด API Routes
* ส่งข้อมูลไปยัง Business Layer
* จัดรูปแบบ HTTP Response
* ไม่ควรมี Business Logic หรือ SQL

**ตัวอย่าง:**

* routes
* controllers
* middlewares

---

### 2️⃣ Business Logic Layer

**โฟลเดอร์:** `src/business`

**Responsibilities:**

* จัดการ Business Rules ของระบบ
* ตรวจสอบความถูกต้องของข้อมูล (Validation)
* คำนวณและตัดสินใจตามเงื่อนไขของระบบ
* ไม่ติดต่อฐานข้อมูลโดยตรง

**ตัวอย่าง:**

* services
* validators

---

### 3️⃣ Data Access Layer

**โฟลเดอร์:** `src/data`

**Responsibilities:**

* ติดต่อฐานข้อมูล SQLite
* เขียน SQL Queries
* แยกการจัดการข้อมูลออกจาก Business Logic
* คืนข้อมูลให้ Business Layer

**ตัวอย่าง:**

* repositories
* database connection

---

## 🔄 Data Flow (Request → Response)

ลำดับการทำงานของระบบมีดังนี้:

1. Client ส่ง HTTP Request (เช่น POST /api/books)
2. Route รับ Request และส่งไปยัง Controller
3. Controller เรียกใช้งาน Service ใน Business Layer
4. Service ตรวจสอบข้อมูลผ่าน Validator
5. Service เรียก Repository ใน Data Layer
6. Repository ติดต่อ SQLite Database
7. Database ส่งผลลัพธ์กลับไปยัง Repository
8. Repository ส่งข้อมูลกลับไปยัง Service
9. Service ส่งผลลัพธ์กลับไปยัง Controller
10. Controller ส่ง HTTP Response กลับไปยัง Client

---

## 🎯 Summary

การใช้ Layered Architecture ช่วยให้ระบบมีโครงสร้างชัดเจน แยกความรับผิดชอบของแต่ละส่วนอย่างเหมาะสม
ทำให้ระบบสามารถดูแลรักษา ขยาย และทดสอบได้ง่ายในระยะยาว

สถาปัตยกรรมนี้เหมาะสมกับระบบ Backend API และเป็นรูปแบบมาตรฐานที่ใช้กันอย่างแพร่หลายในอุตสาหกรรมซอฟต์แวร์
